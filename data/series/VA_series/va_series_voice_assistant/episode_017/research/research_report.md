# 调研报告

从 500ms 缩短到 200ms 的实时语音交互优化，本质上是一场涉及“流式架构、算法加速与网络传输”的全链路极致压榨。核心技术路径包括：采用 WebSocket/WebRTC 建立全双工管道，彻底消除等待；在 LLM 环节引入推测解码与 4-bit 量化（如 AWQ）以成倍降低首字延迟；并逐渐向边缘-云协同及多模态原生模型（如 GPT-4o, AudioPaLM）演进，通过直接处理音频 token 略去中间文本转换环节，最终实现逼近人类真实对话体验的超低延迟交互。
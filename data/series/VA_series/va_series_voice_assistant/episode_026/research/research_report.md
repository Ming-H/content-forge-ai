# 调研报告

在语音对话中，由于‘没有文字可以回看’的瞬时性特征，‘它’等代词和省略语的指代消解是口语理解的核心挑战。传统方案依赖级联架构（ASR+NLU），通过共指消解算法和对话状态追踪（如Rasa框架）处理，但极易受语音识别错误的影响。随着大模型的发展，目前业界正向基于Transformer的注意力上下文传递（历史对话拼接与KV Cache）演进；而在2024-2025的最前沿，端到端语音大模型（如GPT-4o、Qwen-Audio）正彻底颠覆该领域，系统开始直接从音频特征中隐式进行跨模态指代消解，大幅提升了语音交互的自然度与准确率。